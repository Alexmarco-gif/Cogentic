[project]
name = "cogent"
version = "0.1.0"
description = "Cogent Application"
readme = "README.md"
requires-python = ">=3.9"

[tool.ruff]
# Exclude a variety of commonly ignored directories
exclude = [
    ".git",
    ".venv",
    "__pycache__",
    "node_modules",
    "build",
    "dist",
    ".eggs",
    "*.egg-info",
]

line-length = 88
indent-width = 4

# Assume Python 3.9+
target-version = "py39"

[tool.ruff.lint]
# Enable Pyflakes (`F`) and a subset of the pycodestyle (`E`) codes by default
select = [
    "E",     # pycodestyle errors
    "W",     # pycodestyle warnings
    "F",     # Pyflakes
    "I",     # isort
    "N",     # pep8-naming
    "UP",    # pyupgrade
    "B",     # flake8-bugbear
    "C4",    # flake8-comprehensions
    "SIM",   # flake8-simplify
]

ignore = [
    "E501",  # line too long (handled by black)
    "B008",  # do not perform function calls in argument defaults
    "C901",  # too complex
    # Exception chaining: HTTPException re-raises are intentional
    "B904",
    # Stylistic simplifications — defer until post-launch refactor
    "SIM102",  # nested if → single if
    "SIM103",  # return negated condition
    "SIM105",  # try-except-pass → contextlib.suppress (changes async semantics)
    "SIM108",  # if-else → ternary
    "SIM110",  # for loop → any/all
    # Naming conventions — non-breaking, defer until post-launch
    "N806",   # uppercase variable in function (e.g. CONSTANT = ...)
    "N811",   # constant imported as non-constant
    "E741",   # ambiguous variable name (l, O, I)
    "B007",   # loop control variable not used in body
    "B023",   # function in loop doesn't bind variable
    "B027",   # empty method in abstract base class
    # Module-level imports after code (intentional in worker.py, database.py)
    "E402",
    # SQLAlchemy ORM uses == True/False for SQL generation; ruff's suggestion
    # would change the generated SQL semantics on nullable columns
    "E712",
]

# Allow fix for all enabled rules (when `--fix` is provided)
fixable = ["ALL"]
unfixable = []

# Allow unused variables when underscore-prefixed
dummy-variable-rgx = "^(_+|(_+[a-zA-Z0-9_]*[a-zA-Z0-9]+?))$"

[tool.ruff.format]
# Use double quotes for strings
quote-style = "double"

# Indent with spaces, rather than tabs
indent-style = "space"

# Respect magic trailing commas
skip-magic-trailing-comma = false

# Automatically detect the appropriate line ending
line-ending = "auto"

[tool.black]
line-length = 88
target-version = ['py39']
include = '\.pyi?$'
extend-exclude = '''
/(
  # directories
  \.eggs
  | \.git
  | \.hg
  | \.mypy_cache
  | \.tox
  | \.venv
  | build
  | dist
  | node_modules
)/
'''

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
python_files = "test_*.py"
python_classes = "Test*"
python_functions = "test_*"
filterwarnings = [
    "ignore::DeprecationWarning",
    "ignore::UserWarning",
]
markers = [
    "slow: marks tests as slow (deselect with '-m \"not slow\"')",
]

[tool.mypy]
python_version = "3.12"
ignore_missing_imports = true
strict = false
warn_unused_ignores = true
warn_return_any = true
warn_unreachable = true
disallow_untyped_defs = false
check_untyped_defs = true
exclude = [
    "alembic/",
    "tests/",
    "backend/scripts/",
    "backend/ml/training/",
]

[[tool.mypy.overrides]]
module = [
    "backend.services.*",
    "backend.repositories.*",
    "backend.middleware.*",
]
disallow_untyped_defs = true

[tool.coverage.run]
source = ["backend"]
omit = [
    "backend/__pycache__/*",
    "backend/scripts/*",
    "tests/*",
]

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "def __repr__",
    "if __name__ == .__main__.",
    "raise NotImplementedError",
]
